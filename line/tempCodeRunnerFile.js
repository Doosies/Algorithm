else {
        //   for (let j = 0; j < k; j++) {
        //     wordArr.unshift(".");
        //     if (isSame(wordArr, dic[i] && wordArr.length >= dic[i].length)) {
        //       chat[wordIdx] = makeSharp(word.length);
        //     }
        //   }
        